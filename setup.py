from setuptools import setup

description = ['Make path configuration easier']

setup(
    name='most',
    version='1.0',
    python_requires='>=3.9',
    author='ZJU3DV',
    description=description,
    packages=['lib']
)
